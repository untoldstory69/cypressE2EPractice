version: '2.1'
orbs:
  cypress: cypress-io/cypress@3
jobs:
  run-cypress-test:
      - cypress/run-tests:
          cypress-command: npm run test:generatereport
workflows:
  use-my-orb:
    jobs:
      - run-cypress-test:
          name: Run cypress test script
    
