version: '2.1'
orbs:
  cypress: cypress-io/cypress@3
jobs:  
  run-tests:
    executor: cypress/default  
    steps:
      - cypress/run-tests:
          cypress-command: npm run test:generatereport
workflows:
  use-my-orb:
    jobs:
      - run-tests:
          name: Run Tests
