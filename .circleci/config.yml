version: 2.1

orbs:
  cypress: cypress-io/cypress@1.26.0

workflows:
  build:
    jobs:
      - cypress/run:
          cypress-command: npm run test:generatereport 
          post-steps:
           
            - store_artifacts:
                name: Uploading mochawesome-html-report
                path: mochawesome-report/cypress-combined-report.html